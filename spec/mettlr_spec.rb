require "spec_helper"

RSpec.describe Mettlr do
  it "has a version number" do
    expect(Mettlr::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
